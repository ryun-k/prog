<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="qnaBoard">
	
	<!-- 게시물 총 갯수 -->
	<select id="totalCount" resultType="int">
		select count(*) as cnt from qna
	</select>
	
	<!-- 목록보기 -->
	<select id="qnaList" parameterType="qVO" resultType="qVO">
		select * from (
		SELECT no, ROWNUM as rNo, p_no as pNo, g_no as gNo, LPAD(' ',2*(LEVEL-1)) || TITLE as title, 
		m.m_id as id, writedate as wDate, hit
		FROM QNA q, member m
		where q.id=m.m_id
		START WITH p_no=0
		CONNECT BY PRIOR no = p_no
		order SIBLINGS by g_no desc)
		where rNo BETWEEN #{start} and #{end}
	</select>
	
	<!-- 질문입력 쿼리 -->
	
	<insert id="insertQ" parameterType="qVO">
		INSERT INTO QNA VALUES 
		((select nvl(max(no),0)+1 from qna),0, (select nvl(max(no),0)+1 from qna),#{title},#{content},#{id},SYSDATE, 0)
	</insert>
	
	<!-- 조회수 증가  -->	
	<update id="updateHit" parameterType="int">
		update qna set hit = hit+1 where no= #{oriNo}
	</update>
	
	<!-- 상세보기 -->
	<resultMap type="qVO" id="clobView">
		<result property="content" column="content" jdbcType="CLOB" javaType="java.lang.String"/>
	</resultMap>
	<select id="qnaView" resultMap="clobView" parameterType="int">
		select no, p_no as pNo, g_no as pNo, title, content, m.m_id as id, writeDate as wDate
		from qna q, member m
		where no=#{no} and q.id = m.m_id
	</select>
	
	<!-- 답변입력 쿼리 -->
	<insert id="insertA" parameterType="qVO">
		INSERT INTO QNA VALUES 
		((select nvl(max(no),0)+1 from qna), #{oriNo}, #{oriNo},#{title},#{content},#{id},SYSDATE, 0)
	</insert>
	
</mapper>
